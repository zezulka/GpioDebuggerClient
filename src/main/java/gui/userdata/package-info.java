/**
 * This package contains all classes which represent data holders in the user
 * interface, some of which are being read (stored) from (to) XML files.
 * XML files are used for persistent store and are converted into these objects
 * using XStream library.
 */
package gui.userdata;
